{{- if .Values.svc }}
{{- range $v := .Values.svc -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $v.name }}
spec:
  ports:
{{- toYaml $v.ports | indent 4 }}
  selector:
{{ toYaml $v.selector | indent 4 }}
{{- end }}
{{- end }}
